version: '3.7'
services:
  db:
    image: postgres:latest
    ports:
      - 5432:5432
    restart: always
    environment:
    - POSTGRES_PASSWORD=s3ret!
    - POSTGRES_USER=postgres
    - POSTGRES_DB=flyway_test_db_1
